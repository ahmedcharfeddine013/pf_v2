import React from "react";
import { Button } from "./ui/button";

const HeroButton = () => {
  return (
    <Button className="rounded-sm opacity-0 translate-x-32" id="hero-button">
      RESUME
    </Button>
  );
};

export default HeroButton;
